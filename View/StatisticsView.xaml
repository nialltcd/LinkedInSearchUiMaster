<UserControl x:Class="LinkedInSearchUi.View.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
             xmlns:local="clr-namespace:LinkedInSearchUi.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical">
                <Expander Header="View Top Company Job Pairs Statistics">
                    <oxy:PlotView Model="{Binding TopCompanyJobPairsPlot}"/>
                </Expander>
                <Expander Header="View Profile Usefulness Overview Statistics">
                    <oxy:PlotView Model="{Binding ProfileUsefulnessPlot}"/>
                </Expander>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
